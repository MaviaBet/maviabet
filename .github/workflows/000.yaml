name: Flujo de Trabajo de Ejemplo

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Verificar código
        uses: actions/checkout@v2

      - name: Usar Secreto
        run: echo "El secreto es ${{ secrets.BASESCAN_API_KEY }}"
